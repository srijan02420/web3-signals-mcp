# ============================================================
# Web3 Signals x402 — Environment Variables
# Copy this file to .env and fill in your values
# ============================================================

# --- Database ---
# If set, uses Postgres. If not set, falls back to SQLite (signals.db)
# Railway auto-injects this when you add a Postgres addon
# DATABASE_URL=postgresql://user:pass@host:5432/dbname

# --- Server ---
PORT=8000

# --- Whale Agent (Layer 1: Twitter + Layer 2-4: On-chain) ---
APIFY_API_KEY=apify_api_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
ETHERSCAN_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# --- Narrative Agent (Reddit) ---
REDDIT_CLIENT_ID=xxxxxxxxxxxxxx
REDDIT_CLIENT_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# --- Signal Fusion (LLM Insights) ---
# Claude Haiku generates cross-dimensional insights
# Remove or leave blank to disable LLM insights (scores still work)
ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# --- Internal Request Tagging ---
# Test scripts send this header to avoid being counted as external traffic
INTERNAL_API_KEY=change-me-to-a-random-secret

# --- x402 Payment Gate (set PAY_TO to enable) ---
# Enables HTTP 402 micropayments on Base mainnet (USDC)
# Agents discover you on Bazaar and pay per call — no API keys needed
PAY_TO=0xYourBaseMainnetWalletAddress
# Testnet: https://www.x402.org/facilitator (Base Sepolia)
# Mainnet: https://api.cdp.coinbase.com/platform/v2/x402 (Base mainnet)
X402_FACILITATOR_URL=https://api.cdp.coinbase.com/platform/v2/x402

# CDP API keys — required for mainnet facilitator authentication
# Get these from https://cdp.coinbase.com → API Keys
CDP_API_KEY_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
CDP_API_KEY_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Key0 gateway integration
# Shared secret between this FastAPI backend and the Key0 Docker gateway.
# Key0 sends this as X-Key0-Internal-Token on every proxied request.
# Leave empty to disable the check (local dev without Key0).
KEY0_PROXY_SECRET=
# Public URL of your Key0 gateway (shown in 401 error responses)
KEY0_PUBLIC_URL=https://api.web3signals.com
